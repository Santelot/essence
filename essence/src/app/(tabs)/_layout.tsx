// Thin wrapper — the actual tab bar lives in the AppTabs component so the
// logic can be shared / platform-specialized via app-tabs.web.tsx if needed.
export { default } from '@/components/app-tabs';
